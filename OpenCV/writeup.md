# ArUco Marker Detection using OpenCV

This directory contains a Python script that performs real-time detection and pose estimation of ArUco markers through the system webcam.

## Prerequisites
Make sure you have the following installed:
- **Python 3**
- **OpenCV (Contrib version)** — install using:
  ```bash
  pip install opencv-contrib-python

